{
  "schema_version": 1,
  "source": "static_reference",
  "disclaimer": "These figures are STATIC, clearly labeled reference values for router planning — NOT live-fetched from AWS Pricing or CloudWatch. They will drift. Do not bill customers from this file.",
  "as_of": "2026-07",
  "region_hint": "us-east-1 list-price ballpark; verify before decisions",
  "models": {
    "amazon.titan-text-express-v1": {
      "input_usd_per_1k_tokens": 0.0002,
      "output_usd_per_1k_tokens": 0.0006,
      "typical_latency_ms": {
        "p50": 350,
        "p95": 1100
      },
      "notes": "On-demand text; latency is a typical single-request ballpark, not an SLO."
    },
    "anthropic.claude-3-haiku-20240307-v1:0": {
      "input_usd_per_1k_tokens": 0.00025,
      "output_usd_per_1k_tokens": 0.00125,
      "typical_latency_ms": {
        "p50": 450,
        "p95": 1500
      },
      "notes": "Popular low-latency Claude tier on Bedrock; confirm current Bedrock model ID in your account."
    }
  },
  "how_to_replace_with_real_measurements": [
    "Enable Bedrock model access in an AWS account (Console → Bedrock → Model access).",
    "Run a short invoke harness against each model_id (same contract payload as /v1/infer) and record p50/p95 latency from client timestamps or CloudWatch InvocationLatency.",
    "Pull current $/1K input and output token rates from AWS Bedrock pricing for your region (or Cost Explorer after a controlled soak).",
    "Overwrite the models{} entries here (keep source/disclaimer fields) or point the phase-13 router at an externally maintained pricing map.",
    "Never refresh this file from CI — that would require live account credentials (ADR-002)."
  ]
}
