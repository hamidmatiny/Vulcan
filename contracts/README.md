# contracts

Platform contracts for Vulcan. Phase 0 ships **`model-contract`** — the backend-agnostic OpenAPI + JSON Schema every serving runtime must implement ([ADR-001](../docs/adr/001-unified-model-serving-contract.md)).

| Package | Purpose |
|---------|---------|
| [`model-contract/`](./model-contract/) | `/health`, `/metrics`, `/v1/infer`, resource-requirements |
