apiVersion: v2
name: vulcan-gpu-operator-overlays
description: Vulcan-owned ConfigMaps / overlays for NVIDIA GPU Operator (values validated separately)
type: application
version: 0.7.0
appVersion: "24.9.0"
