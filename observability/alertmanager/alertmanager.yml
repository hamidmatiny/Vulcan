global:
  resolve_timeout: 5m

route:
  receiver: vulcan-null
  group_by: [alertname, job]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  # Local compose: log-only sink (no external pages). Wire Slack/PagerDuty in real envs.
  - name: vulcan-null
