{
  "model_id": "reference-tiny-llm",
  "backend": "sagemaker",
  "gpu_memory_mib": {
    "min": 0,
    "max": 16384
  },
  "supports_mig": false,
  "supports_quantization": false,
  "cold_start_seconds": {
    "min": 60,
    "max": 600
  },
  "cpu_dev_mode": true,
  "notes": "Managed SageMaker Endpoint for reference-tiny-llm. cpu_dev_mode=true describes the simulated train/eval path used in CI (moto). Real GPU endpoint shapes are manual-only (see docs/runbooks/sagemaker-manual-run.md). GET /v1/resources on a contract shim would surface this manifest; native InvokeEndpoint is the managed equivalent of POST /v1/infer."
}
