# CPU-only runtime for Vulcan Ray Serve adapter (ADR-002).
# Install torch separately from the CPU wheel index (see Dockerfile / README).
setuptools>=69,<82
# CVE-2025-62593 fixed in 2.52.0. Cap <2.56 to keep pickle-safe FastAPI/Starlette pins
# (Ray 2.56+ requires fastapi>=0.133). Dockerfile sets
# PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python so protobuf>=5 upb does not break Serve.
ray[serve]>=2.52.0,<2.53
# FastAPI/Starlette pins: newer Starlette embeds locks that Ray cannot pickle for @serve.ingress.
fastapi>=0.110,<0.116
starlette>=0.36,<0.38
uvicorn>=0.30
transformers>=4.40,<4.47
safetensors>=0.4
onnxruntime>=1.17,<1.20
Pillow>=10.0
numpy>=1.26,<2.0
prometheus-client>=0.20
httpx>=0.27
jsonschema>=4.22
PyYAML>=6.0
# Coherent OTel line (avoids pip backtracking across incompatible 1.x minors).
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-http==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
