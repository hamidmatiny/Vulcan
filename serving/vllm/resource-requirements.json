{
  "model_id": "reference-tiny-llm",
  "backend": "vllm",
  "gpu_memory_mib": {
    "min": 0,
    "max": 0
  },
  "supports_mig": true,
  "supports_quantization": true,
  "cold_start_seconds": {
    "min": 10,
    "max": 180
  },
  "cpu_dev_mode": true,
  "notes": "LLM-only adapter. CPU-dev uses an OpenAI-compatible small-model path (phase-1 GPT-2) for contract correctness — not GPU continuous batching / PagedAttention. GPU vllm serve is manual (ADR-002); see README."
}
